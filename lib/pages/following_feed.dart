import 'package:flutter/material.dart';

class FollowingFeed extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Following Feed Page'),
      ),
    );
  }
  
}